# Email Composer

A small email sending system: an Express API that sends email over SMTP (with CC/BCC, HTML or plain text, and attachments), and a Next.js frontend to compose and send.

> Work in progress — built in phases, see [`phase.md`](phase.md). Full setup and docs land in Phase 9.
